# Polycentria-Governance — conversion & compatibility study

How the BioChain Enterprise governance system converts into the **Polycentria
governance topology** (the six Points: Relation → Interpretation → Intersection →
Reconciliation → Control, with Calibration as the dial; the three geometric invariants
A1/A2/A3; the tamper-evidence security property) — what each side adds to the other,
and the implementation path.

**Read `index.html`** — the full comparison page. Summary of findings:

- **Verdict: deeply compatible, three hard gates.** The two systems are convergent
  evolution: a 64-bit packet as the atom of verifiable state, untrusted proposer split
  from exact certifier (our Polycentria Dual-Helix ↔ their six Points), honesty as an
  engineering discipline (their tags ↔ our measured benches).
- **Conversion is a mapping, not a rewrite** — every Point already has a measured
  BioChain mechanism living at it (§02 of the page).
- **BioChain → Polycentria:** chiral+mirror holonomy for composition *history* (their
  A3 order-free pooling has no who-composed-when fingerprint), the chiral pairing rule
  as Reconciliation *mechanized* (self-attestation impossible by type), pump-clock
  logical time for their swarm milestone, the predict-then-correct residual channel
  aimed at their 12/36 SW-gate-missing windows, the FLUX/CRYST rail under
  Calibration·Compression·Compliance.
- **Polycentria → BioChain:** the honesty-tag vocabulary, the real-data boundary
  discipline (our benches are synthetic — EuRoC MH_01 is the fix), SW(H₁) as a third
  crystallization score, the adapter contract, the Points as a design-review checklist.
- **The three gates (must clear before code crosses):** the naming collision
  ("Polycentria" model vs org; the two document sets contradict each other on Schism
  Labs) · the IP assignment chain for the Hyperbolic Sparsemax Pump (originates here,
  tracked in their internal inventory — merging code is a legal event until resolved
  by counsel) · disclosure hygiene (this repo is public GitHub Pages; internal/legal
  materials informed the gates but are not reproduced).

Implementation phases (page §06): **0** clear the gates → **1** vocabulary merge
(tags, Points, joint conformance contract — docs only) → **2** one real stream, three
experiments (EuRoC MH_01 through the engram codec; residual channel vs their gate-miss
windows; SW(H₁) score) → **3** interfaces (Form-ID dialect, adapter contract, signed
gate verdicts) → **4** governance merge (holonomy beside monoid, pairing rule, pump
windows as Point of Control timing) → **5** the economy.

*Claims on the page carry Polycentria's honesty tags ([IN-CODE], [TO-SUBSTANTIATE],
[ASPIRATIONAL], [OPEN-PROBLEM]). No confidential material is reproduced.*
